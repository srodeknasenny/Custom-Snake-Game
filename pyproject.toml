[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "custom-snake"
version = "1.0.0"
description = "Custom Matplotlib Snake"
dependencies = [
    "matplotlib"
]

[project.scripts]
play-snake = "snake.main:run"